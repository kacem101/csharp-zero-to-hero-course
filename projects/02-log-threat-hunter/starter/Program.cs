using System;

class Program
{
    static void Main(string[] args)
    {
        string path = args.Length > 0 ? args[0] : "sample-data/auth.log";

        // TODO: parse the file, run both detectors, print a summary
        // report (top offending IPs, brute-force alerts, impossible
        // travel alerts).
    }
}
