using System;

class Program
{
    static void Main()
    {
        var analyzer = new PasswordAnalyzer();
        Console.WriteLine("Password Strength Auditor — type 'exit' to quit.");

        // TODO: loop reading passwords from Console.ReadLine(), print
        // entropy, detected patterns, and overall classification for
        // each one, until the user types "exit".
    }
}
