using PasswordManager.Domain;
using Xunit;

public class PasswordGeneratorTests
{
    // TODO: [Fact] Generate(12, includeSymbols: false) returns a string
    // of length 12 containing only letters/digits.

    // TODO: [Fact] two calls to Generate produce DIFFERENT passwords
    // (proves it's not returning a fixed/predictable value).
}
