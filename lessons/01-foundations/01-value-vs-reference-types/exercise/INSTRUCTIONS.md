# Exercise — Lesson 01 — Value Types vs Reference Types

## Practice

Create a new console project and work through these tasks in `Program.cs`.

Fill in the `// TODO` items in `Program.cs`, then run:

```bash
dotnet run
```
