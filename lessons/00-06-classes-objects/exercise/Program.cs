// TODO 1: Write the `Node` class and `LinkedList` class shown above (or
// your own version), then create a list, add the values 3, 2, 1 (in
// that order, using AddFront), and print it. Expected output order:
// 1 -> 2 -> 3 -> null

// TODO 2: Add a method `int Count()` to LinkedList that traverses the
// list and returns how many nodes it has, without modifying any
// existing method.

// TODO 3: Add a method `bool Contains(int value)` that returns true if
// any node holds that value.

// TODO 4: Demonstrate reference semantics: create two Node variables
// where one is assigned from the other, mutate the Data field through
// one variable, and print both to confirm they show the same value
// (just like the `a`/`b` example above, but with your own Node class).

// TODO 5 (conceptual, answer in a comment): in C, forgetting to free a
// linked list after you're done with it is a memory leak you have to
// hunt down yourself. What handles this in C#, and what do you, the
// programmer, no longer need to do?
