// TODO 1: Write FizzBuzz for numbers 1 to 30 using a `for` loop and
// if/else (print "Fizz" for multiples of 3, "Buzz" for multiples of 5,
// "FizzBuzz" for both, otherwise the number).

// TODO 2: Given int[] scores = { 55, 72, 88, 91, 40, 67 }, use a
// `foreach` loop to print each score, and separately use a `for` loop to
// print each score together with its index ("Score at index 0: 55").

// TODO 3: Write a switch EXPRESSION `string SizeLabel(int liters)` that
// returns "small" for < 1, "medium" for 1-2, "large" for > 2 (you can
// use range patterns like in Lesson 04, or plain conditions with `when`
// if you haven't reached Lesson 04 yet — either is fine here).

// TODO 4: Using an int[] of 10 elements, print the last 3 elements using
// range syntax (`nums[^3..]`), without a loop.
