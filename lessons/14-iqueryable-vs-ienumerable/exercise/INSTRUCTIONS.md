# Exercise — Lesson 14 — IQueryable vs IEnumerable

## Practice

Since this needs a real EF Core database to demonstrate fully, work through this conceptually with comments — that's the actual skill being tested here.

Fill in the `// TODO` items in `Program.cs`, then run:

```bash
dotnet run
```
